export default defineAppConfig({
  name: "Admin",
  ui: { primary: "violet" },
});
/*
declare module '@nuxt/schema' {
  interface AppConfigInput {
    myLayer?: {
      / * * Project name * /
      name?: string
    }
  }
}
*/
