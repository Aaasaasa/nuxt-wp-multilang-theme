datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client/default"
}

model DfUser {
  id    String @id @default(cuid())
  name  String
  email String @unique
}
