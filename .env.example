# Database Configuration
# Main database connection (required)

DB_PREFIX=np_

POSTGRES_HOST="localhost"
POSTGRES_PORT="5432"
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="P@ssw0rd"
POSTGRES_DB="database"


# Test database connection (required for testing)
# Uses a separate database to avoid conflicts during E2E tests
TEST_DATABASE_URL="postgresql://postgres:P@ssw0rd@localhost:5432/test_nuxt-multilang-theme-develop"

# MySQL database connection (optional, for additional features)
# Environment Configuration
MYSQL_HOST="localhost"
MYSQL_PORT="3306"
MYSQL_USER="user"
MYSQL_PASSWORD="password"
# MySQL database name
MYSQL_NAME="nuxt-multilang-theme-develop"

DB_PREFIX=np_
AUTH_ORIGIN=http://127.0.0.1:3000

# Connection URLs
POSTGRES_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}
MYSQL_URL=mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@localhost:${MYSQL_PORT}/${MYSQL_NAME}
REDIS_URL=redis://localhost:6379

# Test database connection (required for testing)
# Uses a separate database to avoid conflicts during E2E tests
TEST_DATABASE_URL="postgresql://postgres:P@ssw0rd@localhost:5432/test_nuxt-multilang-theme-develop"



API_BASE_URL=
API_SECRET=

# Application Environment
# Set to 'production' in production environments, NODE_ENV="development" is for development
NODE_ENV="development"

# Optional: Enable debug mode for Nuxt

# Optional: Custom port for development server

# Security Configuration
# CORS origins for production (comma-separated list)

# Authentication Configuration
# Session password for secure cookies (required for authentication)
# Generate with: openssl rand -base64 32
NUXT_SESSION_PASSWORD="password-with-at-least-32-characters"

# SEO Configuration
# Public site URL for canonical URLs and Open Graph tags
NUXT_PUBLIC_SITE_URL="http://localhost:3000"

# Production only variables (uncomment and configure for production)
